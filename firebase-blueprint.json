{
  "entities": {
    "Dataset": {
      "title": "Dataset",
      "description": "Represents a data source with columns and rows",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "columns": { "type": "array", "items": { "type": "string" } },
        "data": { "type": "array", "items": { "type": "object" } },
        "rowCount": { "type": "number" },
        "updatedAt": { "type": "number" }
      },
      "required": ["id", "name", "columns", "data"]
    },
    "Dashboard": {
      "title": "Dashboard",
      "description": "Contains a list of tiles/widgets for the dashboard layout",
      "type": "object",
      "properties": {
        "tiles": { 
          "type": "array", 
          "items": { "type": "object" }
        },
        "updatedAt": { "type": "number" }
      },
      "required": ["tiles"]
    }
  },
  "firestore": {
    "/users/{userId}/datasets/{datasetId}": {
      "schema": "Dataset",
      "description": "Store individual datasets owned by a user"
    },
    "/users/{userId}/dashboards/{dashboardId}": {
      "schema": "Dashboard",
      "description": "Store dashboard layouts owned by a user"
    }
  }
}
